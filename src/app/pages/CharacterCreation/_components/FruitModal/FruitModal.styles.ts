import { css } from "@emotion/react";

const fruitAbility = css`
  text-align: center;
  margin: 2rem 0 0.5rem 0;
  color: var(--foreground-primary);
`;

const fruitHelperText = css`
  text-align: center;
  color: var(--foreground-secondary);
`;

export { fruitAbility, fruitHelperText };
